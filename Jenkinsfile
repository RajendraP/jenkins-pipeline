@Library('Library@master') _

node {  
    stage('Build') { 
        echoVar 'Build'
        // 
    }
    stage('Test') { 
        echoVar 'Test'
        // 
    }
    stage('Deploy') { 
        echoVar 'Deploy'
        // 
    }
}
